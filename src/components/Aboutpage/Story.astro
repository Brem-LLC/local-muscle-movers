---
const { block } = Astro.props
---

<section>
    <div class="mx-auto grid w-full max-w-7xl grid-cols-1 items-center gap-12 px-7 py-16 lg:grid-cols-2 lg:px-10">
        <div class="">
            <h1 class="mb-8 text-3xl font-bold md:text-5xl">{block.heading}</h1>
            <p class="text-lg leading-relaxed">
                {block.description}
            </p>
        </div>

        <div>
            <style>.embed-container { position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; max-width: 100%; } .embed-container iframe, .embed-container object, .embed-container embed { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }</style><div class='embed-container'><iframe src=`https://www.youtube.com/embed/${block.video}` frameborder='0' allowfullscreen></iframe></div>
        </div>
    </div>
</section>